[project]
name = "nooa-mini-lab"
version = "0.1.0"
description = "Minimal NVIDIA NOOA experiment demonstrating an agent that inspects and modifies live Python state through object-oriented actions."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nooa>=0.0.10",
]
